{
  "name": "fake-features-server",
  "version": "1.0.0",
  "description": "Serve set of random features using the json-server",
  "main": "index.js",
  "scripts": {
    "start": "node index.js --middlewares middleware.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Stepan Kuzmin <to.stepan.kuzmin@gmail.com> (stepankuzmin.ru)",
  "license": "MIT",
  "dependencies": {
    "faker": "3.1.0",
    "json-server": "0.8.22"
  }
}
